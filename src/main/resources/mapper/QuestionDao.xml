<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.demo_sb.dao.QuestionDao">

    <select id="queryQuestion" resultType="com.sp.demo_sb.entity.Question">
        SELECT id,
            papper_id,
            create_time,
            question_type,
            question_title,
            question_option
        FROM question
        ORDER BY id DESC
    </select>

    <select id="queryQusetionByPaperId" resultType="com.sp.demo_sb.entity.Question">
        SELECT id,
            papper_id,
            create_time,
            question_type,
            question_title,
            question_option
        FROM question
        WHERE
        papper_id=#{papperId}
    </select>

    <select id="queryQuestionById" resultType="com.sp.demo_sb.entity.Question">
        SELECT id,
        papper_id,
        create_time,
        question_type,
        question_title,
        question_option
        FROM question
        WHERE
        id=#{id}
    </select>

    <insert id="insertQuestion" keyProperty="id" keyColumn="id"
            parameterType="com.sp.demo_sb.entity.Question" >
        INSERT INTO question(
            id,
            papper_id,
            create_time,
            question_type,
            question_title,
            question_option)
        VALUES
        (#{id}, #{papperId},#{createTime}, #{questionType}, #{questionTitle},#{questionOption})
    </insert>

    <update id="updateQuestion" parameterType="com.sp.demo_sb.entity.Question">
        UPDATE question
        <set>
            <if test="papperId != null">papper_id = #{papperId},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="questionType != null">question_type = #{questionType},</if>
            <if test="questionTitle != null">question_title = #{questionTitle},</if>
            <if test="questionOption != null">question_option = #{questionOption}</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteQuestion">
        DELETE FROM
        question
        WHERE id = #{id}
    </delete>

</mapper>